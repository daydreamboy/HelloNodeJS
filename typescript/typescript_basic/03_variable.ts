// Note: not use var
let aVar: string = 'JavaScript';
aVar = 'TypeScript';

const aConst: string = 'Hello';
// Error: const variable can't be modified
//aConst = 'hello';

console.log(aConst + ', ' + aVar + '!');
